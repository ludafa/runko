# Base image version must match the @cloudflare/sandbox npm package version
# in package.json — mismatched SDK/container versions are the most common
# source of cryptic runtime errors with this SDK.
FROM docker.io/cloudflare/sandbox:0.12.3

# Documents the ports this application uses (standard Docker convention)
EXPOSE 8080
