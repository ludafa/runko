/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import { apiErrorSchema } from './apiErrorSchema.ts';
import { postChatMessageInputSchema } from './postChatMessageInputSchema.ts';
import { startTurnAckSchema } from './startTurnAckSchema.ts';
import { z } from 'zod/v4';

export const postApiChatConversationsIdMessagesPathParamsSchema = z.object({
  id: z.string(),
});

/**
 * @description Accepted — see `mode` (\"started\" | \"steered\" | \"queued\"); poll/stream `GET .../stream` for its events. Stopping a turn while it is still being assembled still works, but the ack for that message is \"started\" — the stopped-turn frames arrive on the stream like any other outcome
 */
export const postApiChatConversationsIdMessages202Schema = z.lazy(
  () => startTurnAckSchema,
);

/**
 * @description Unauthorized
 */
export const postApiChatConversationsIdMessages401Schema = z.lazy(
  () => apiErrorSchema,
);

/**
 * @description Not found
 */
export const postApiChatConversationsIdMessages404Schema = z.lazy(
  () => apiErrorSchema,
);

/**
 * @description Either the 待发队列 is full (docs/features/steer-and-queue.md §2.3 — nothing is ever silently dropped), or a turn was already in progress and could not be steered either (narrow race — the turn ended between the steer attempt and the fallback start)
 */
export const postApiChatConversationsIdMessages409Schema = z.lazy(
  () => apiErrorSchema,
);

/**
 * @description Model/sandbox configuration or provisioning error
 */
export const postApiChatConversationsIdMessages500Schema = z.lazy(
  () => apiErrorSchema,
);

/**
 * @description The server is shutting down (docs/tech/graceful-shutdown.md §3.3) — no new turn is accepted during shutdown. Retryable: resend once the new process is up
 */
export const postApiChatConversationsIdMessages503Schema = z.lazy(
  () => apiErrorSchema,
);

export const postApiChatConversationsIdMessagesMutationRequestSchema = z.lazy(
  () => postChatMessageInputSchema,
);

export const postApiChatConversationsIdMessagesMutationResponseSchema = z.lazy(
  () => postApiChatConversationsIdMessages202Schema,
);
