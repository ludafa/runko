/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@kubb/plugin-client/clients/fetch';
import type {
  GetApiPushConfigQueryResponse,
  GetApiPushConfig401,
} from '../types/GetApiPushConfig.ts';
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from '@kubb/plugin-client/clients/fetch';

function getGetApiPushConfigUrl() {
  const res = { method: 'GET', url: `/api/push/config` as const };
  return res;
}

/**
 * @summary 推送是否可用 + VAPID 公钥（docs/app/push-notification/tech.md §3.1）：前端据此决定渲不渲染铃铛
 * {@link /api/push/config}
 */
export async function getApiPushConfig(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetApiPushConfigQueryResponse,
    ResponseErrorConfig<GetApiPushConfig401>,
    unknown
  >({
    method: 'GET',
    url: getGetApiPushConfigUrl().url.toString(),
    ...requestConfig,
  });
  return res.data;
}
