/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@kubb/plugin-client/clients/fetch';
import type {
  GetApiConsoleOverviewQueryResponse,
  GetApiConsoleOverview401,
} from '../types/GetApiConsoleOverview.ts';
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from '@kubb/plugin-client/clients/fetch';

function getGetApiConsoleOverviewUrl() {
  const res = { method: 'GET', url: `/api/console/overview` as const };
  return res;
}

/**
 * @summary 集群控制台：节点列表 + 每个节点下正在跑的会话（技术方案 §8）
 * {@link /api/console/overview}
 */
export async function getApiConsoleOverview(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetApiConsoleOverviewQueryResponse,
    ResponseErrorConfig<GetApiConsoleOverview401>,
    unknown
  >({
    method: 'GET',
    url: getGetApiConsoleOverviewUrl().url.toString(),
    ...requestConfig,
  });
  return res.data;
}
