/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@kubb/plugin-client/clients/fetch';
import type {
  GetApiChatConfigQueryResponse,
  GetApiChatConfig401,
} from '../types/GetApiChatConfig.ts';
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from '@kubb/plugin-client/clients/fetch';

function getGetApiChatConfigUrl() {
  const res = { method: 'GET', url: `/api/chat/config` as const };
  return res;
}

/**
 * @summary 这台服务端能做什么：能选哪几档沙盒、用的是真模型还是演示模型（docs/ingress/tech/unified-demo.md §4.3）
 * {@link /api/chat/config}
 */
export async function getApiChatConfig(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetApiChatConfigQueryResponse,
    ResponseErrorConfig<GetApiChatConfig401>,
    unknown
  >({
    method: 'GET',
    url: getGetApiChatConfigUrl().url.toString(),
    ...requestConfig,
  });
  return res.data;
}
