{
  "type": "object",
  "properties": {
    "ok": { "type": "boolean", "enum": [true] },
    "mode": {
      "type": "string",
      "enum": ["started", "steered", "queued", "aborted"]
    }
  },
  "required": ["ok", "mode"],
  "x-readme-ref-name": "StartTurnAck"
}
