# 拷成 .dev.vars（已 gitignore）供 `wrangler dev` 本地注入。
#
# Worker 里没有 process.env / --env-file 那一套：本地开发的密钥走 .dev.vars，
# 线上走 `wrangler secret put`。这正是 Node 版服务端（apps/node-server）上
# Workers 时要改造的点之一（现服务端有 19 个 process.env 变量，全都得改走
# bindings/secrets）。
#
# 值可以直接从仓库根 .env 里抄过来。

# ---- 角色 ①：Worker 自己跑 agent 会话（POST /agent 需要） ----
DEEPSEEK_API_BASE_URL=https://api.deepseek.com
DEEPSEEK_API_TOKEN=
# 可选，默认 deepseek-chat
RUNKO_MODEL=

# ---- 角色 ②：对外 BYO 网关端点（ALL /gateway/* 需要） ----
# 一个强随机值；外部 Node 客户端 cloudflareWorkspace({ token }) 要用同一个值。
# 不填则 /gateway/* 返回 503（刻意不退化成无鉴权）。线上用：
#   wrangler secret put RUNKO_GATEWAY_TOKEN
RUNKO_GATEWAY_TOKEN=
