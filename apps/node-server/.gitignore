node_modules
dist

# env files (commit only the example)
.env
.env.local
.env.production

# sqlite runtime
*.db
*.db-shm
*.db-wal

# `openapi.yml` is committed — it's the contract that the client's Kubb
# codegen consumes. Regenerate with `pnpm --filter @runko-chat/node-server
# run generate:openapi`.

# chat-smoke.sh 的本地状态（cookie / 当前会话 id）
scripts/.smoke/
