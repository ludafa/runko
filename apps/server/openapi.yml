openapi: 3.1.0
info:
  title: Hono Mono Starter API
  version: 1.0.0
components:
  schemas:
    Note:
      type: object
      properties:
        id:
          type: string
        title:
          type: string
        body:
          type: string
        createdAt:
          type: string
      required:
        - id
        - title
        - body
        - createdAt
    ApiError:
      type: object
      properties:
        error:
          type: string
          description: Error message
          examples:
            - Not found
      required:
        - error
    CreateNoteInput:
      type: object
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 255
        body:
          type: string
      required:
        - title
        - body
    ChatSession:
      type: object
      properties:
        id:
          type: string
        title:
          type: string
        repo:
          type: string
        branchName:
          type: string
        sandboxName:
          type: string
        status:
          type: string
          enum:
            - active
            - sleeping
            - expired
        lastActiveAt:
          type: string
        createdAt:
          type: string
      required:
        - id
        - title
        - repo
        - branchName
        - sandboxName
        - status
        - lastActiveAt
        - createdAt
    CreateChatSessionInput:
      type: object
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 255
    ChatEventsList:
      type: object
      properties:
        events:
          type: array
          items:
            $ref: "#/components/schemas/ChatEventEnvelope"
      required:
        - events
    ChatEventEnvelope:
      type: object
      properties:
        seq:
          type: integer
        event:
          anyOf:
            - type: object
              properties:
                type:
                  type: string
                  enum:
                    - session.started
                sessionId:
                  type: string
              required:
                - type
                - sessionId
            - type: object
              properties:
                type:
                  type: string
                  enum:
                    - turn.started
                turn:
                  type: number
              required:
                - type
                - turn
            - type: object
              properties:
                type:
                  type: string
                  enum:
                    - item.started
                item:
                  anyOf:
                    - type: object
                      properties:
                        id:
                          type: string
                        type:
                          type: string
                          enum:
                            - agent_message
                        text:
                          type: string
                      required:
                        - id
                        - type
                        - text
                    - type: object
                      properties:
                        id:
                          type: string
                        type:
                          type: string
                          enum:
                            - reasoning
                        text:
                          type: string
                      required:
                        - id
                        - type
                        - text
                    - type: object
                      properties:
                        id:
                          type: string
                        type:
                          type: string
                          enum:
                            - user_message
                        text:
                          type: string
                      required:
                        - id
                        - type
                        - text
                    - type: object
                      properties:
                        id:
                          type: string
                        type:
                          type: string
                          enum:
                            - tool_call
                        toolName:
                          type: string
                        input: {}
                        output:
                          anyOf:
                            - type: string
                            - {}
                            - type: "null"
                        status:
                          type: string
                          enum:
                            - in_progress
                            - completed
                            - failed
                            - denied
                      required:
                        - id
                        - type
                        - toolName
                        - status
                    - type: object
                      properties:
                        id:
                          type: string
                        type:
                          type: string
                          enum:
                            - file_change
                        changes:
                          type: array
                          items:
                            type: object
                            properties:
                              path:
                                type: string
                              kind:
                                type: string
                                enum:
                                  - add
                                  - update
                                  - delete
                            required:
                              - path
                              - kind
                      required:
                        - id
                        - type
                        - changes
                    - type: object
                      properties:
                        id:
                          type: string
                        type:
                          type: string
                          enum:
                            - plan_update
                        items:
                          type: array
                          items:
                            type: object
                            properties:
                              text:
                                type: string
                              completed:
                                type: boolean
                            required:
                              - text
                              - completed
                      required:
                        - id
                        - type
                        - items
                    - type: object
                      properties:
                        id:
                          type: string
                        type:
                          type: string
                          enum:
                            - error
                        message:
                          type: string
                      required:
                        - id
                        - type
                        - message
              required:
                - type
                - item
            - type: object
              properties:
                type:
                  type: string
                  enum:
                    - item.updated
                item:
                  anyOf:
                    - type: object
                      properties:
                        id:
                          type: string
                        type:
                          type: string
                          enum:
                            - agent_message
                        text:
                          type: string
                      required:
                        - id
                        - type
                        - text
                    - type: object
                      properties:
                        id:
                          type: string
                        type:
                          type: string
                          enum:
                            - reasoning
                        text:
                          type: string
                      required:
                        - id
                        - type
                        - text
                    - type: object
                      properties:
                        id:
                          type: string
                        type:
                          type: string
                          enum:
                            - user_message
                        text:
                          type: string
                      required:
                        - id
                        - type
                        - text
                    - type: object
                      properties:
                        id:
                          type: string
                        type:
                          type: string
                          enum:
                            - tool_call
                        toolName:
                          type: string
                        input: {}
                        output:
                          anyOf:
                            - type: string
                            - {}
                            - type: "null"
                        status:
                          type: string
                          enum:
                            - in_progress
                            - completed
                            - failed
                            - denied
                      required:
                        - id
                        - type
                        - toolName
                        - status
                    - type: object
                      properties:
                        id:
                          type: string
                        type:
                          type: string
                          enum:
                            - file_change
                        changes:
                          type: array
                          items:
                            type: object
                            properties:
                              path:
                                type: string
                              kind:
                                type: string
                                enum:
                                  - add
                                  - update
                                  - delete
                            required:
                              - path
                              - kind
                      required:
                        - id
                        - type
                        - changes
                    - type: object
                      properties:
                        id:
                          type: string
                        type:
                          type: string
                          enum:
                            - plan_update
                        items:
                          type: array
                          items:
                            type: object
                            properties:
                              text:
                                type: string
                              completed:
                                type: boolean
                            required:
                              - text
                              - completed
                      required:
                        - id
                        - type
                        - items
                    - type: object
                      properties:
                        id:
                          type: string
                        type:
                          type: string
                          enum:
                            - error
                        message:
                          type: string
                      required:
                        - id
                        - type
                        - message
              required:
                - type
                - item
            - type: object
              properties:
                type:
                  type: string
                  enum:
                    - item.completed
                item:
                  anyOf:
                    - type: object
                      properties:
                        id:
                          type: string
                        type:
                          type: string
                          enum:
                            - agent_message
                        text:
                          type: string
                      required:
                        - id
                        - type
                        - text
                    - type: object
                      properties:
                        id:
                          type: string
                        type:
                          type: string
                          enum:
                            - reasoning
                        text:
                          type: string
                      required:
                        - id
                        - type
                        - text
                    - type: object
                      properties:
                        id:
                          type: string
                        type:
                          type: string
                          enum:
                            - user_message
                        text:
                          type: string
                      required:
                        - id
                        - type
                        - text
                    - type: object
                      properties:
                        id:
                          type: string
                        type:
                          type: string
                          enum:
                            - tool_call
                        toolName:
                          type: string
                        input: {}
                        output:
                          anyOf:
                            - type: string
                            - {}
                            - type: "null"
                        status:
                          type: string
                          enum:
                            - in_progress
                            - completed
                            - failed
                            - denied
                      required:
                        - id
                        - type
                        - toolName
                        - status
                    - type: object
                      properties:
                        id:
                          type: string
                        type:
                          type: string
                          enum:
                            - file_change
                        changes:
                          type: array
                          items:
                            type: object
                            properties:
                              path:
                                type: string
                              kind:
                                type: string
                                enum:
                                  - add
                                  - update
                                  - delete
                            required:
                              - path
                              - kind
                      required:
                        - id
                        - type
                        - changes
                    - type: object
                      properties:
                        id:
                          type: string
                        type:
                          type: string
                          enum:
                            - plan_update
                        items:
                          type: array
                          items:
                            type: object
                            properties:
                              text:
                                type: string
                              completed:
                                type: boolean
                            required:
                              - text
                              - completed
                      required:
                        - id
                        - type
                        - items
                    - type: object
                      properties:
                        id:
                          type: string
                        type:
                          type: string
                          enum:
                            - error
                        message:
                          type: string
                      required:
                        - id
                        - type
                        - message
              required:
                - type
                - item
            - type: object
              properties:
                type:
                  type: string
                  enum:
                    - turn.completed
                usage:
                  $ref: "#/components/schemas/Usage"
              required:
                - type
                - usage
            - type: object
              properties:
                type:
                  type: string
                  enum:
                    - turn.failed
                error:
                  $ref: "#/components/schemas/NimboError"
              required:
                - type
                - error
            - $ref: "#/components/schemas/ChatUserMessage"
            - $ref: "#/components/schemas/ChatTurnResult"
            - $ref: "#/components/schemas/ChatTurnFailed"
            - $ref: "#/components/schemas/ChatApprovalRequested"
            - $ref: "#/components/schemas/ChatApprovalResolved"
            - $ref: "#/components/schemas/ChatQuestionAsked"
            - $ref: "#/components/schemas/ChatQuestionAnswered"
      required:
        - event
    Usage:
      type: object
      properties:
        inputTokens:
          type: number
        outputTokens:
          type: number
        totalTokens:
          type: number
        cachedInputTokens:
          type: number
    NimboError:
      type: object
      properties:
        code:
          type: string
          enum:
            - max_turns
            - context_overflow
            - provider_error
            - aborted
        message:
          type: string
      required:
        - code
        - message
    ChatUserMessage:
      type: object
      properties:
        type:
          type: string
          enum:
            - user.message
        text:
          type: string
      required:
        - type
        - text
    ChatTurnResult:
      type: object
      properties:
        type:
          type: string
          enum:
            - turn.result
        finalResponse:
          type: string
        usage:
          $ref: "#/components/schemas/Usage"
      required:
        - type
        - finalResponse
        - usage
    ChatTurnFailed:
      type: object
      properties:
        type:
          type: string
          enum:
            - turn.failed
        code:
          type: string
        message:
          type: string
      required:
        - type
        - code
        - message
    ChatApprovalRequested:
      type: object
      properties:
        type:
          type: string
          enum:
            - approval.requested
        callId:
          type: string
        toolName:
          type: string
        input: {}
      required:
        - type
        - callId
        - toolName
    ChatApprovalResolved:
      type: object
      properties:
        type:
          type: string
          enum:
            - approval.resolved
        callId:
          type: string
        behavior:
          type: string
          enum:
            - allow
            - deny
        message:
          type: string
      required:
        - type
        - callId
        - behavior
    ChatQuestionAsked:
      type: object
      properties:
        type:
          type: string
          enum:
            - question.asked
        callId:
          type: string
        question:
          type: string
        options:
          type: array
          items:
            type: string
      required:
        - type
        - callId
        - question
    ChatQuestionAnswered:
      type: object
      properties:
        type:
          type: string
          enum:
            - question.answered
        callId:
          type: string
        outcome:
          type: string
          enum:
            - answered
            - timeout
        answer:
          type: string
      required:
        - type
        - callId
        - outcome
    StartTurnAck:
      type: object
      properties:
        ok:
          type: boolean
          enum:
            - true
        mode:
          type: string
          enum:
            - started
            - steered
      required:
        - ok
        - mode
    PostChatMessageInput:
      type: object
      properties:
        text:
          type: string
          minLength: 1
      required:
        - text
    ApprovalAck:
      type: object
      properties:
        ok:
          type: boolean
          enum:
            - true
      required:
        - ok
    PostApprovalInput:
      type: object
      properties:
        behavior:
          type: string
          enum:
            - allow
            - deny
        message:
          type: string
      required:
        - behavior
    PostAnswerInput:
      type: object
      properties:
        answer:
          type: string
          minLength: 1
      required:
        - answer
  parameters: {}
paths:
  /api/notes:
    get:
      tags:
        - Notes
      summary: List the current user’s notes
      responses:
        "200":
          description: Notes
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Note"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      tags:
        - Notes
      summary: Create a note
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateNoteInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Note"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/chat/sessions:
    post:
      tags:
        - Chat
      summary: Create a chat session (provisions a Vercel sandbox + a dedicated git
        branch)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateChatSessionInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatSession"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "500":
          description: Repo/sandbox configuration or provisioning error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    get:
      tags:
        - Chat
      summary: List the current user’s chat sessions
      responses:
        "200":
          description: Sessions
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ChatSession"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/chat/sessions/{id}:
    get:
      tags:
        - Chat
      summary: Get one chat session’s detail (including status)
      parameters:
        - schema:
            type: string
            examples: &a1
              - 3f1b2c4d-...
          required: true
          name: id
          in: path
      responses:
        "200":
          description: Session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatSession"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/chat/sessions/{id}/events:
    get:
      tags:
        - Chat
      summary: Replay a session’s persisted agent events (reconnection/history)
      parameters:
        - schema:
            type: string
            examples: *a1
          required: true
          name: id
          in: path
        - schema:
            type:
              - integer
              - "null"
            minimum: 0
          required: false
          name: after
          in: query
      responses:
        "200":
          description: Events, in seq order
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatEventsList"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/chat/sessions/{id}/messages:
    post:
      tags:
        - Chat
      summary: 'Start a turn for this message, or steer an in-progress one (STEER-3B):
        if the session has a turn running, `text` is injected into it via
        `Session.steer()` (mode "steered"); otherwise this acquires the sandbox,
        builds the session, and hands off to the in-process turn runner (mode
        "started"). Either way, events arrive over `GET .../stream`, not this
        response'
      parameters:
        - schema:
            type: string
            examples: *a1
          required: true
          name: id
          in: path
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PostChatMessageInput"
      responses:
        "202":
          description: Accepted — see `mode` ("started" | "steered"); poll/stream `GET
            .../stream` for its events
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StartTurnAck"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: A turn is already in progress for this session and could not be
            steered either (narrow race — the turn ended between the steer
            attempt and the fallback start)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "500":
          description: Model/sandbox configuration or provisioning error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/chat/sessions/{id}/stream:
    get:
      tags:
        - Chat
      summary: "Resumable live tail of a session’s in-progress turn (docs/08 §2.2b):
        replays persisted events after `after`, then forwards the turn’s live
        events until it ends — reconnect-safe (page refresh/HMR/network blip
        never lose events)"
      parameters:
        - schema:
            type: string
            examples: *a1
          required: true
          name: id
          in: path
        - schema:
            type:
              - integer
              - "null"
            minimum: 0
          required: false
          name: after
          in: query
      responses:
        "200":
          description: "SSE stream of `{ seq, event }` frames: replay, then live tail
            (closes once the turn ends, or immediately after replay if no turn
            is in progress). Some frames omit `seq` (docs/08 §2.2d) — those are
            ephemeral `item.updated` typewriter ticks, never persisted and never
            replayed; every other frame always carries one"
          content:
            text/event-stream:
              schema:
                $ref: "#/components/schemas/ChatEventEnvelope"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/chat/sessions/{id}/approvals/{callId}:
    post:
      tags:
        - Chat
      summary: 批准或拒绝一个待处理的工具调用审批请求（docs/08 §2.2c（审批链））：结果通过 `GET .../stream` 的
        `approval.resolved` 事件送达，本响应只是一个 ack
      parameters:
        - schema:
            type: string
            examples: &a2
              - 3f1b2c4d-...
          required: true
          name: id
          in: path
        - schema:
            type: string
            examples: &a3
              - call_1
          required: true
          name: callId
          in: path
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PostApprovalInput"
      responses:
        "200":
          description: Resolved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApprovalAck"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found — either this session doesn’t exist (or isn’t the
            caller’s), or `callId` has no pending approval on it (already
            resolved, timed out, or never existed)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/chat/sessions/{id}/questions/{callId}:
    post:
      tags:
        - Chat
      summary: 回答一个待处理的 ask_user 提问（docs/08 §2.2c（审批链））：结果通过 `GET .../stream` 的
        `question.answered` 事件送达，本响应只是一个 ack
      parameters:
        - schema:
            type: string
            examples: *a2
          required: true
          name: id
          in: path
        - schema:
            type: string
            examples: *a3
          required: true
          name: callId
          in: path
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PostAnswerInput"
      responses:
        "200":
          description: Resolved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApprovalAck"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Not found — either this session doesn’t exist (or isn’t the
            caller’s), or `callId` has no pending question on it (already
            answered, timed out, or never existed)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
webhooks: {}
